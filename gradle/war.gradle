apply plugin: "war"

bootWar {
    mainClass = "com.jhipster.ecommerce.notification.NotificationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
